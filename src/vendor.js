/* Entry point for Webpack to create a separate bundle for vendor libraries
* Library changes are less frequent, so it's unlikely that a user should have to constantly
 * re-download vendor.js. I */

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
